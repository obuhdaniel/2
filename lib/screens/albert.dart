import 'package:flutter/material.dart';

class AlbertsPage extends StatelessWidget {
  const AlbertsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(' ST ALBERT UNIBEN/UBTH'),
      ),
      body: Center(
        child: Text('PASTORAL COUNCIL'),
      ),
    );
  }
}
